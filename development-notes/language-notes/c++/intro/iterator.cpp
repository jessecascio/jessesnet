#include <iostream>
#include <vector>
using namespace std;

// g++ -std=c++11 intro.cpp -o name
// ./name

/*
	- used to fetch elements and to move from one element to the next
	- we dont not use address of to get iterators
	- .begin() first element, .end() one past the last (the same value for empty iterators, empty - off the end iterators)
	- can use ++ and -- to move through iterators
	- iterators more common the using subscripts []
	- common to use != with for loops not <, as most containers know how to check equivalency
	- .begin() returns vector<int>::iterator
*/
int main()
{
	
}

